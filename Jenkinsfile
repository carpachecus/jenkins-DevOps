pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				echo "Building the app"
			}
		}	
		stage ('Test') {
			steps {
				echo "Testing the app"
			}
		}
		stage ('Integration test') {
			steps {
				echo "Integration the app"
			}
		}
	}
	
}
